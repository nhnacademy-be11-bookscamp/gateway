server:
  port: 8080

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

spring:
  application:
    name: gateway
  cloud:
    gateway:
      server:
        webmvc:
          routes:
            - id: api-service
              uri: lb://api-server
              predicates:
                 - Path=/api-server/**
              filters:
                 - StripPrefix=1

                ## TODO: 인증 서버 추가


